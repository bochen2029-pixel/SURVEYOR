# Site-specific material — NEVER ships. The only place names/roles from any real
# organization may live. If this repo goes public, _local/ stays behind.
_local/

# Local scratch
*.tmp
*.bak
__pycache__/
.DS_Store

# Generated corpora (large; regenerate from the seed)
experiments/f-fixture/out/

# Pinned regulatory sources: fetched locally, never redistributed from this repo.
# crosswalk/sources.yml carries the urls and hashes needed to rebuild it.
crosswalk/corpus/
