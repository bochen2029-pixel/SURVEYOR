# STATE - generated fold. DO NOT EDIT. (gates.py G-FOLD enforces)
tape: 1ef2cc1dba8f8457 | as-of: 2026-09-03T08:19:39+00:00 | events: 317 | sessions: 15 (open: 0)

**NEXT ACTION:** S11. (a) THE SECOND HALF OF THE AUDIT'S ADVICE: run the cold-start audit again, this time against a FROZEN COMMIT with no concurrent writer, and see what a clean read finds. (b) THIRTEEN checks now name a pinned source with no mapping (the number rose from 6 because the 17 rewritten authority strings changed what the coverage fold can see) - python crosswalk/pins.py --coverage lists them; each is one file and one byte-match. (c) RUN THE KIT AGAINST A FRESH HARNESS - still the real rung-09 decider, still not done: give an agent only AGENTS.md and an invented programme's answers, have it author checks, grade with conformance/run.py. (d) Point the local app at a real on-disk tape rather than the generator. Rung 08 (the watch tier) ships off by default and must beat floor-plus-cron; rungs 04 and 05's deciders wait on a real programme. Boot per BOOT.md - and note step 2 now runs `python _build/fold.py --check-open`, which reads the tape rather than a fold that is stale exactly when it matters.

## Rung ladder (status = latest tape verdict of each executioner)
| Rung | Deliverable | Executioner | Status |
|---|---|---|---|
| 00 | Repo skeleton, spec, catalog, prereg, build discipline | G-COLDSTART | PASS |
| 01 | Tape + floor engine + fixtures runner | F-FIXTURE | PASS |
| 02 | Catalog encoded (59 checks, pass+fail fixtures each) | G-CATALOG-COMPLETE | PASS |
| 03 | Clocks engine (STN port, anchor declarations) | G-ANCHOR-PLANTS | PASS |
| 04 | F-RETRO on historical charts (on site, local) | F-RETRO | NOT-RUN |
| 05 | Crosswalk MVP (pinning, mappings, one edition diff) | F-CROSSWALK | NOT-RUN |
| 06 | Ledger + CAPA lifecycle + the three folds | G-FOLD-DETERMINISM | PASS |
| 07 | Morning Board rendered real from the tape | G-EVIDENCE-LINKS | PASS |
| 08 | Watch tier in shadow | F-WATCH-GATE | NOT-RUN |
| 09 | Kit hardening (elicit/, examples, foreign harness) | G-FOREIGN-HARNESS | PASS |

## Open questions (20)
- [2026-09-03T06:22:09+00:00] F-FIXTURE-WORLD is graded on a world this project authored; a generator and a floor written by the same hand can share a blind spot. The honest counterweight is a second author for the generator (or a site's real export at rung 04). Should S4 commission an independent generator pass before rung 04, or does F-RETRO subsume it?
- [2026-09-03T06:38:14+00:00] Answered by the crosswalk, for the R1 reviewer to confirm: 42 CFR 486 Subpart G contains no explicit annual governing-board presentation clause for the QAPI plan (searched 486.324 and 486.348). SV-084's yearly clock is therefore site governance, not federal - retag L2, or name the accreditation standard it comes from
- [2026-09-03T06:38:15+00:00] Fourteen checks rest on FDA 21 CFR 1271 or AATB standards, neither of which is in the pinned corpus, so their authority cannot be byte-checked today. 21 CFR 1271 is public and fetchable; AATB standards are licensed. Pin 21 CFR 1271 next session, and decide what an unpinnable licensed standard means for an L0 tag
- [2026-09-03T06:53:32+00:00] Six checks still rest on authority outside the corpus, all AATB standards, which are licensed and cannot be pinned or redistributed. What does an unpinnable licensed standard mean for an L0 tag? Options: retag L1 with the standard named in prose, or let a site that holds a licence pin it privately in its own corpus root (the multi-root loader now supports exactly that)
- [2026-09-03T07:11:45+00:00] SV-058 is BROADER than both its sources: the guidance scopes the 18-month and breastfed limbs to children of mothers with or at risk for HIV, and the regulation substitutes the birth mother only at one month or younger, while the check requires both interviews for every donor up to 18 months. A defensible site tightening, but it must be a stated one - R1
- [2026-09-03T07:33:48+00:00] Six checks still rest on AATB standards, which are licensed and cannot be pinned or redistributed. The multi-root corpus loader means a site holding a licence can pin them privately; the alternative is retagging those checks L1 with the standard named in prose. R1 decision
- [2026-09-03T07:43:24+00:00] The kit has never been run by a harness that is not this one. G-COLDSTART proved a fresh agent could ORIENT from the repo; nobody has yet asked a fresh agent to COMPLETE A FIT under AGENTS.md and graded what it produced. That is the real rung-09 decider and it costs one subagent session
- [2026-09-03T07:54:28+00:00] The application reads a SYNTHETIC tape because that is the only tape that exists. Pointing it at a real one is a single substitution - World builds its events from the generator, and every view is a pure function of an event list - but nobody has done it against ledger/tape.py's on-disk hash-chained segments, so the read path is unexercised
- [2026-09-03T08:19:10+00:00] I kept writing to the repository while the cold-start audit was reading it - the auditor opened by saying so, and one finding (a NameError on a half-landed gate_witness) was an artifact of that. The audit is still sound because every finding was re-verified against the final state, but the protocol is wrong: a cold-start audit should run against a frozen commit, not a live tree. Freeze next time
- [2026-09-03T08:19:11+00:00] G-EVIDENCE-LINKS can byte-compare the board in site/surveyor.html but not the DEPLOYED copies, which live outside the repository and cannot be reached on a stranger's machine. tools/publish_board.py --check reports their staleness; nothing enforces it. That gap is how the live page sat a week behind

## Open blockers (0)

## Review queue - decisions since last signature (49)
- [2026-09-03T07:33:47+00:00] The page's footer GitHub link now points at the SURVEYOR repository rather than the profile, and the board chrome shows the tape id rather than a hand-written clock | why: The repo did not exist when the page was written (operator). And the chrome and the banner were briefly showing two different tape ids after a re-run - an inconsistent number on a page about honest numbers, now rewritten every run | revert: git revert
- [2026-09-03T07:43:22+00:00] G-FOREIGN-HARNESS loads conformance/run.py BY PATH, never by module name | why: On its first run the gate imported experiments/f-fixture/run.py instead - the name collides - graded the wrong module and returned green. A gate that can be satisfied by a name collision is not a gate. Found in the same session it was written, by reading the traceback rather than the verdict | revert: n/a
- [2026-09-03T07:43:23+00:00] Law B5 is enforced mechanically only when a draft check DECLARES corrects_field and verification_fields; a draft declaring neither passes that test unexamined and the conformance report says so | why: SV-062 needs a subject to read. Implying coverage the kit does not have would be worse than the gap: the honest form is a check that runs when it can and a report that names when it could not | revert: n/a
- [2026-09-03T07:54:24+00:00] The application is read-only by construction and loopback-only by construction: GET is the only method (POST/PUT/DELETE/PATCH return 405 with the reason), the bind is 127.0.0.1 and never 0.0.0.0, there is no document root so no path traversal exists, and the page references nothing off this machine - inline stylesheet, system fonts, and a Content-Security-Policy of default-src 'none' | why: Law A5: the quality record is written by people, and an interface that could write it is the one thing this architecture forbids. SPEC section 11: zero egress, air-gap supported - which is a claim about a socket, not a sentiment, so it is held at the socket. The public product page loads a font service; this deliberately does not, because an air-gapped workstation must render it identically | revert: n/a
- [2026-09-03T07:54:25+00:00] G-EVIDENCE-LINKS extended to grade the application: every rendered link must resolve, the app must reference nothing off the machine, and every write method must be refused | why: A dead citation is worse than a printed one, because it looks checkable. Making the evidence clickable is only an improvement if the clicks land | revert: drop the ui half of the gate
- [2026-09-03T07:54:26+00:00] The check page lists under 'cases it acted on' only the cases where the check RETURNED ITS ACTION; abstentions are counted separately and are not actions | why: The first render put every register and referral in a table headed 'cases it acted on' because the filter was `verdict != PASS`. A CANNOT-EVALUATE is the floor saying it cannot judge, and calling that an action is the imprecision this whole project exists to remove | revert: n/a
- [2026-09-03T07:54:27+00:00] World indexes the tape once (results by check) and route() memoises pages; the selftest builds a smaller world and bounds its crawl | why: The first selftest ran past two minutes: every check page rescanned the whole tape, and the link crawl multiplied that by the number of links. Pages are pure functions of an immutable World, so memoising them is sound rather than a shortcut | revert: n/a
- [2026-09-03T08:19:02+00:00] THE EXIT POLICY: anything that is not PASS now fails the build, CANNOT-EVALUATE included, with an explicit --allow-cannot-evaluate GATE escape that must name the gate | why: THE AUDIT'S HEADLINE FINDING, and it was correct. Every gate reaches its organ through try/import/except-CANNOT-EVALUATE, and only FAIL set the exit code - so DELETING AN ORGAN turned its gate silent and the battery still exited 0. Ten of thirteen gates could be neutralised by breaking an import. Law A4 says a gate that cannot judge must say so; the implementation said so and then shipped it as success | revert: git revert (not recommended: this was the largest hole in the fence)
- [2026-09-03T08:19:03+00:00] G-WITNESS accepts only FAIL, never CANNOT-EVALUATE, and the G-CATALOG-COMPLETE perturbation was sharpened to shrink the catalog rather than merely silence the gate | why: Worse than the finding above: the meta-gate I had just written accepted silence as refusal, so it certified '12/12 gates refused a known-bad copy' when ten of them had merely gone quiet. Every perturbation leaves the organ present and damages only what the gate guards, so a gate that goes silent on one cannot see its own subject | revert: n/a
- [2026-09-03T08:19:04+00:00] The single-writer lock is read FROM THE TAPE: python _build/fold.py --check-open, and BOOT step 2 now says so | why: BOOT told a new session to detect a concurrent writer by reading STATE.md - but STATE only learns of a session when that session folds, and a session still working has by construction not folded. The auditor hit it live: STATE said open 0, a fresh fold of the same tape said open 1, and they walked into a repository another agent was writing. The one law with a named mechanical enforcer for concurrency failed in precisely its intended case | revert: n/a
- [2026-09-03T08:19:05+00:00] The product tape takes each event's own timestamp; the ledger chain head is now an identity, pinned in gates.py by tools/pin_ledger_head.py and enforced | why: Every record was stamped datetime.now() and the chain hash covers the record, so the head differed on every run over identical events - and the build published it in its verdict as though it were a tamper-evidence receipt. A receipt that changes when nothing changed identifies nothing | revert: unpin (loses the property)
- [2026-09-03T08:19:06+00:00] Six further audit fixes: G-PRIVACY scans .jsonl (it had never scanned the TAPE, the primary write target, and 36 previously invisible warn-tier hits appeared the moment it did); the catalog count is a pinned constant, so a catalog that shrinks fails rather than reporting 58/58; the survey binder VERIFIES each citation and drops the unverified, and carries the currency warning it used to swallow; G-EVIDENCE-LINKS re-renders and byte-compares the published board rather than shape-checking it; RESULTS.md is guarded by F-FIXTURE-WORLD; AGENTS.md now names the right enforcer per artifact | why: Each was a document or a gate overstating a receipt. The binder one is the worst of them: it would have handed a surveyor a citation to a superseded CMS outcome regime with no caveat, on a machine with no corpus at all | revert: per-fix
- [2026-09-03T08:19:07+00:00] The multi-seed sweep is now computed and recorded (run.py --sweep): 8 seeds, 1,416 plants, 1,416 caught, 0 missed, 0 false holds in 43,906 clean pairs. AGENTS.md's 'across 8 seeds' previously sat on a single seed's numbers | why: I had run the sweep at S3 and never recorded it, then attached its adjective to another run's numbers in the one file addressed to foreign harnesses, under a heading saying 'you may rely on it'. A robustness claim needs a receipt, not a recollection | revert: n/a
- [2026-09-03T08:19:08+00:00] SV-028 retagged L0 -> L1/L2, and the stale 'pin at rung 05' promise replaced in 17 checks | why: SV-028's entire authority is AATB, a licensed voluntary accreditation standard - not federal rule, OPTN policy or state statute - so a surveyor asking which federal rule requires 15 hours would have got no answer. The 17 promises pointed at a rung whose decider is NOT-RUN and, for the AATB subset, can never land. Retagging made SV-028 a variation point, which the kit immediately noticed: conformance failed until its elicit question was written | revert: per-check
- [2026-09-03T08:19:09+00:00] denylist.example.txt ships, G-PRIVACY's message names it, and README states the two steps a fresh clone needs (copy the denylist, run tools/fetch_corpus.py --fetch) | why: The auditor's verdict was that a stranger can orient here faster than in almost any repository they had read, and then cannot RUN it: no denylist means the privacy gate fails closed, no corpus means the crosswalk cannot verify, and nothing said so anywhere | revert: n/a

## Last 8 mounts
- [2026-09-03T07:33:44+00:00] _build/gates.py (+G-EVIDENCE-LINKS) - Rung 07's executioner: the renderer's battery, plus a check that the PUBLISHED page carries a generated board and that every row of it carries the evidence element - a green renderer over a stale page would still be a false page
- [2026-09-03T07:43:16+00:00] elicit/questions.yml + elicit/method.md - One question per variation point, keyed to the 14 checks tagged L2, L0/L2 or L1/L2, each with ask / sources / impact / trap / mandated_floor. `sources` lists documents and timestamps BEFORE it lists people, because people report the rule they believe and the record carries the rule that operated - and the difference between those is the finding. Every question names the clause its duty comes from, so answering an L0/L2 question sets the site's number without relaxing the mandated floor underneath it. Usable with no software at all
- [2026-09-03T07:43:17+00:00] examples/worked/ (1 accepted, 6 refused) + REFUSED.md - THE HALF THAT TRANSFERS. Six refusals, each declaring in its own `refusal:` field the class it must be refused as, and conformance re-derives the refusal and compares - a narrated refusal nobody re-runs decays into fiction. Four of the six are well-formed, TRUE and wrong: restating a mandated check (a second divergent copy of the law), firing where it does not apply, authoring L0, and verifying a correction through the field being corrected. That last one is caught by running SV-062 - a floor check whose subject is a check definition - against the draft, using the same engine that runs every other check
- [2026-09-03T07:43:18+00:00] conformance/run.py - The one command a foreign harness runs: the seed intact, the accepted example mounts, every refused draft refused for its declared reason, every variation point carrying a question, and adapters/ empty by design. It reports what is HERE and says so - trust it over any prose in the repository, including AGENTS.md
- [2026-09-03T07:43:19+00:00] AGENTS.md + adapters/CONTRACT.md - The boot contract: what state this is in (including what is NOT established and why the two reasons are not equivalent), the layer matrix (L2/L3 only, never L0/L1, never L4), the four laws a competent harness breaks anyway - each one naming the defect this project actually shipped - and the instruction to stop at green, because a signature is the only way across. The adapter contract assumes the worst case (read-only, export-grade, roles never names, clock battery first) and adapters/ stays empty because inventing a binding is the fabrication the citation gate exists to prevent
- [2026-09-03T07:43:20+00:00] _build/gates.py (+G-FOREIGN-HARNESS) - Rung 09's executioner: the kit graded by the command a foreign harness would actually run
- [2026-09-03T07:54:23+00:00] ui/serve.py + ui/README.md - The local application: a stdlib http.server on 127.0.0.1:8731 that serves the same folds live and makes every citation a LINK. Click the handles beside a number and you land on the tape events behind it; click a check id and you get its predicate, its anchor and why that anchor, the clause it runs under byte-matched to a pinned source, its fixtures, and every verdict it returned. Seven routes, all rendered from memory. Nothing to install: stdlib only, so it runs on a workstation with no package manager, no internet and no administrator
- [2026-09-03T08:18:59+00:00] _build/witness.py + _build/gates.py (+G-WITNESS) - Law D11's executioner, named at S0 and built here: for each gate a named minimal perturbation of the thing it guards, applied to a throwaway copy of the repository in a subprocess; the gate must report FAIL. 12/12 proven. It proves itself first, against a copy whose G-CATALOG was rewritten to always return PASS, and reports G-COLDSTART as unwitnessable-by-machine rather than skipping it. A full member of the battery rather than an opt-in flag, on purpose - a meta-gate that only runs when asked has the exact defect it was built to fix. ~37s

