# SV-071 - the authorization record is complete before allocation begins: every
# required field (named in the site's required-field list) carries a value on the
# authorization itself - one home per fact. Harvested [H]: the new record system gates this
# at entry; the floor makes the gate portable.
id: SV-071
title: authorization record field-complete before allocation starts
family: reconciliation
layer: L0
authority: "State Revised Uniform Anatomical Gift Act documentation + OPTN Policy 2.2 OPO responsibilities (authorization before allocation); site SOP [H] - AUTHORITY OUTSIDE THE PINNED CORPUS (AATB and FDA guidance are licensed or non-codified; see crosswalk/pins.py --coverage). Not a promise of a future pin"
trigger: on_close_attempt
predicate: "count(authorization.required_fields) > 0 and every(authorization.required_fields, exists(authorization[value]))"
action: hold
message: "Authorization cannot close for allocation: a required field is blank."
evidence: [authorization.required_fields, authorization]
expires: P2Y
inverse: retire
tests: fixtures/SV-071/
